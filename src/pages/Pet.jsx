import { NavLink } from "react-router";

const Pet = () => {
  // pet

  return (
    <div className="details-container">
      <h1>Pet page</h1>
      <p>{pet.name}</p>
    </div>
  );
};

export default Pet;
